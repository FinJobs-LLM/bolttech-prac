# Copy to .env and fill in. .env is git-ignored and loaded automatically by
# src/config.py (via python-dotenv) for the FastAPI servers.

# Required only for the LLM explanation endpoint (GET /explain).
OPENAI_API_KEY=
